# Customization Rules

- **Auto-Deploy/Push**: After implementing any changes or bug fixes, always commit the changes and push them to GitHub (`git push`) to trigger the Vercel automatic deployment. Do not wait for the user to ask for deployment.
